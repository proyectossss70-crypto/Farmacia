rules_version = '2';

// Toda la lectura/escritura pasa por el servidor (Firebase Admin SDK),
// que ignora estas reglas. Bloqueamos el acceso directo desde clientes.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
